import type { Box, CrossSection, Manifold, ManifoldToplevel, Mesh } from 'manifold-3d';
export type { Box as ManifoldBox, CrossSection, Manifold, Mesh };
export type GeometryWasm = ManifoldToplevel;
export type DisposableGeometry = CrossSection | Manifold;
